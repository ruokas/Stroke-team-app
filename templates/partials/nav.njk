<nav id="mainNav" role="tablist">
  <a
    id="activation-tab"
    href="#activation"
    data-section="activation"
    class="tab btn active"
    role="tab"
    ><span class="tab-icon" aria-hidden="true">{{ icon('activation') | safe }}</span><span class="tab-label">Aktyvacija</span></a
  >
  <a
    id="arrival-tab"
    href="#arrival"
    data-section="arrival"
    class="tab btn"
    role="tab"
    ><span class="tab-icon" aria-hidden="true">{{ icon('arrival') | safe }}</span><span class="tab-label">Paciento atvykimas</span></a
  >
  <a
    id="nihss-tab"
    href="#nihss"
    data-section="nihss"
    class="tab btn"
    role="tab"
    ><span class="tab-icon" aria-hidden="true">{{ icon('nihss') | safe }}</span><span class="tab-label">NIHSS</span></a
  >
  <a
    id="thrombolysis-tab"
    href="#thrombolysis"
    data-section="thrombolysis"
    class="tab btn"
    role="tab"
    ><span class="tab-icon" aria-hidden="true">{{ icon('thrombolysis') | safe }}</span><span class="tab-label">Pasiruošimas trombolizei</span></a
  >
  <a
    id="decision-tab"
    href="#decision"
    data-section="decision"
    class="tab btn"
    role="tab"
    ><span class="tab-icon" aria-hidden="true">{{ icon('decision') | safe }}</span><span class="tab-label">Sprendimas</span></a
  >
  <a
    id="complications-tab"
    href="#complications"
    data-section="complications"
    class="tab btn"
    role="tab"
    ><span class="tab-icon" aria-hidden="true">{{ icon('warning') | safe }}</span><span class="tab-label">Komplikacijos</span></a
  >
  <a
    id="summarySec-tab"
    href="#summarySec"
    data-section="summarySec"
    class="tab btn"
    role="tab"
    ><span class="tab-icon" aria-hidden="true">{{ icon('summary') | safe }}</span><span class="tab-label">Santrauka</span></a
  >
</nav>
